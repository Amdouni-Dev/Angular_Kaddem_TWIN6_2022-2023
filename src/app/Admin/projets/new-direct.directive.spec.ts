import { NewDirectDirective } from './new-direct.directive';

describe('NewDirectDirective', () => {
  it('should create an instance', () => {
    const directive = new NewDirectDirective();
    expect(directive).toBeTruthy();
  });
});
